import { ValidatMaxDatePipe } from './validat-max-date.pipe';

describe('ValidatMaxDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidatMaxDatePipe();
    expect(pipe).toBeTruthy();
  });
});
